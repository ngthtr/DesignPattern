When we have a class with many parameter in construction
We creat a class builder to build main class
this class as a buffer for main class
with method build() return main class we will have a class same as we create a normal class